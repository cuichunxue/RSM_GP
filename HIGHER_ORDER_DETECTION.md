# 高次項検知機能

## 概要

`model.diagnose()` に**高次項検知機能**を追加しました。RSMで捉えられない3次以上の非線形項をGPが補正している場合、ユーザーに自動的に通知します。

---

## 背景と目的

### 問題

RSMは**二次多項式**までしかモデル化できません：
- 線形項: x₁, x₂, ...
- 交互作用項: x₁×x₂, ...
- 二次項: x₁², x₂², ...

しかし、実際の現象には**3次以上の高次項**が含まれることがあります：
- 3次項: x₁³, x₂³, x₁²×x₂, ...
- 4次項: x₁⁴, x₂⁴, ...
- sin/cos などの完全に非線形な関数

### 解決策

**GPが高次項を補正**している場合、その存在をユーザーに通知：

```
【高次項検知】
  ⚠ 高次項（3次以上）の存在: 強い
  GP補正（平均）: 1.6365
  GP補正（最大）: 5.4772
  補正の相対値: 応答範囲の7.9%
  → RSMでは捉えられない重要な非線形性が存在します
  → GPがこの高次項を効果的に補正しています
```

---

## 検知ロジック

### 判定基準

| 強度 | GP寄与度 | GP改善率 | 意味 |
|------|---------|---------|------|
| **強い** | >30% | >15% | 高次項が支配的、RSM単独では不十分 |
| **中程度** | >15% | >8% | 高次項が重要、GPで補正が効果的 |
| **弱い** | >5% | >3% | 軽微な高次項、ほぼRSMで説明可能 |
| **なし** | ≤5% | ≤3% | 高次項なし、RSMで十分 |

### 計算方法

```python
# 1. RSMのみの予測
y_rsm_only = RSM(X)

# 2. RSM+GPの予測
y_full = RSM(X) + GP(X)

# 3. GP補正の大きさ
gp_correction = y_full - y_rsm_only

# 4. 寄与度計算
rsm_var = Var(y_rsm_only - mean(y_true))
gp_var = Var(gp_correction)
gp_contribution = gp_var / (rsm_var + gp_var) * 100

# 5. 改善率計算
r2_full = R²(y_true, y_full)
r2_rsm = R²(y_true, y_rsm_only)
gp_improvement = (r2_full - r2_rsm) * 100
```

---

## 使い方

### 基本的な使用

```python
import rsm

# モデル訓練
model = rsm.RSMPlusGP_Production(...)
model.fit(X_train, y_train)

# 診断（高次項検知を含む）
info = model.diagnose(X_test, y_test, display=True)
```

### 出力情報

```python
# 返り値の辞書に以下が追加:
info['higher_order_detected']      # bool: 高次項が検出されたか
info['higher_order_strength']       # str: "強い"/"中程度"/"弱い"/"なし"
info['gp_correction_avg']          # float: GP補正の平均値
info['gp_correction_max']          # float: GP補正の最大値
info['gp_correction_relative']     # float: 応答範囲に対する相対値(%)
info['gp_correction_cv']           # float: 補正の変動係数
```

### プログラムによる判定

```python
info = model.diagnose(X_test, y_test, display=False)

if info['higher_order_detected']:
    if info['higher_order_strength'] == "強い":
        print("警告: 高次項が支配的です")
        print("→ データを増やすとより正確に捕捉")
    elif info['higher_order_strength'] == "中程度":
        print("注意: 高次項が存在します")
    else:
        print("軽微な高次項あり")
else:
    print("高次項なし、RSMで十分")
```

---

## 出力例

### ケース1: 高次項なし（純粋2次関数）

```
【高次項検知】
  ✓ 高次項の存在は検出されませんでした
  → RSMの二次多項式で十分に説明可能
```

### ケース2: 弱い高次項

```
【高次項検知】
  △ 高次項（3次以上）の存在: 弱い
  GP補正（平均）: 0.6954
  GP補正（最大）: 1.7159
  補正の相対値: 応答範囲の4.4%
  → 軽微な非線形性のみ、ほぼRSMで説明可能

【推奨事項】
  ○ 軽微な高次項の可能性があります
    GP寄与度: 4.1%（小さい）
```

### ケース3: 中程度の高次項

```
【高次項検知】
  ○ 高次項（3次以上）の存在: 中程度
  GP補正（平均）: 0.8765
  GP補正（最大）: 2.1432
  補正の相対値: 応答範囲の6.2%
  → 軽微〜中程度の高次項が存在する可能性があります

【推奨事項】
  ○ 高次項（3次以上）の存在が示唆されます
    GP寄与度: 12.3%、改善率: 9.5%
    → データを増やすとより正確に高次項を捕捉（n≥100推奨）
```

### ケース4: 強い高次項（sin/cos関数）

```
【高次項検知】
  ⚠ 高次項（3次以上）の存在: 強い
  GP補正（平均）: 1.6365
  GP補正（最大）: 5.4772
  補正の相対値: 応答範囲の7.9%
  → RSMでは捉えられない重要な非線形性が存在します
  → GPがこの高次項を効果的に補正しています

【推奨事項】
  ⚠ 高次項（3次以上）の存在が強く示唆されます
    GP寄与度: 14.4%（RSMで捉えられない非線形性）
    GP改善率: 15.0%（高次項の重要性が高い）
    → 現在のデータ量（n=150）で高次項を良好に捕捉
    → GP補正の大きさ: 応答範囲の7.9%
```

---

## 検証結果

### テストケース（test_strong_nonlinearity.py）

| ケース | GP寄与 | GP改善 | 検知 | 評価 |
|--------|--------|--------|------|------|
| sin/cos関数 | 14.4% | 15.0% | **強い** | ✓ 正しく検知 |
| 大係数高次項 | 4.1% | 4.7% | 弱い | ✓ 適切に検知 |
| 3次支配 | 7.3% | 7.9% | 弱い | ✓ 適切に検知 |
| 純粋2次 | 0.0% | -0.0% | なし | ✓ 正しく検知せず |

### 実測データ（analyze_higher_order.py より）

| データ数 | GP改善率 | 評価 |
|---------|---------|------|
| n=30 | 11% | データ不足で限定的 |
| n=100 | **68%** | **高次項を効果的に捕捉** |
| n=200 | **90%** | **高次項を十分に捕捉** |

---

## ユーザー体験

### Before（従来）

ユーザーは高次項の存在に気づかない：
```
R² = 0.85  # RSMのみ
```
→ 「精度が低い」としか分からない

### After（高次項検知機能）

ユーザーに高次項の存在を通知：
```
【高次項検知】
  ⚠ 高次項（3次以上）の存在: 強い
  → RSMでは捉えられない重要な非線形性が存在します
  → GPがこの高次項を効果的に補正しています

【予測性能】
  RSMのみのR²: 0.8493
  RSM+GPのR²:  0.9997
  GP改善:      15.0%
```
→ 「高次項が存在し、GPが補正している」と理解できる

### 推奨事項も自動生成

```
【推奨事項】
  ⚠ 高次項（3次以上）の存在が強く示唆されます
    GP寄与度: 14.4%（RSMで捉えられない非線形性）
    GP改善率: 15.0%（高次項の重要性が高い）
    → 現在のデータ量（n=150）で高次項を良好に捕捉
```

---

## データ量と検知精度

### データ量の影響

| データ数 | 検知能力 | 推奨 |
|---------|---------|------|
| n<50 | 限定的 | データ不足、高次項を誤検知または見逃す可能性 |
| n=50-100 | 中程度 | 強い高次項のみ検知、弱い高次項は見逃す可能性 |
| n≥100 | 良好 | ほとんどの高次項を検知可能 |
| n≥150 | 非常に良好 | 高精度で高次項を検知 |

### 推奨事項

少数データで高次項が検知されない場合：
```
【高次項検知】
  ✓ 高次項の存在は検出されませんでした
  → RSMの二次多項式で十分に説明可能
  → データを増やすと潜在的な高次項を検知可能（n≥100推奨）

【推奨事項】
    GPの改善が小さいです（0.5%）
    → データを増やすとGPが高次項を捕捉可能（n≥100推奨）
```

---

## 技術的詳細

### なぜGP補正が高次項を示すのか

**RSMの限界**:
- 二次多項式: y = β₀ + Σβᵢxᵢ + ΣΣβᵢⱼxᵢxⱼ + Σβᵢᵢxᵢ²
- 3次以上は表現不可

**GPの役割**:
- RSMの残差（y_true - y_rsm）をモデル化
- 残差が大きい = RSMで捉えられない構造が存在
- RBFカーネルで非線形性を柔軟に表現

**検知ロジック**:
- GP補正が大きい → 高次項または非線形性が存在
- GP改善率が高い → その非線形性が重要

### 誤検知の可能性

**False Positive（誤検知）**:
- ノイズが大きい場合
  → GPがノイズを補正して見える
  → WhiteKernelで軽減
- データが少ない場合（n<50）
  → GPが過学習
  → カーネルパラメータ評価で検出

**False Negative（見逃し）**:
- データが少ない場合（n<50）
  → GPが高次項を捕捉できない
  → 推奨事項で警告
- カーネルが過平滑化
  → length_scaleが大きすぎる
  → カーネルパラメータ評価で検出

---

## よくある質問

### Q1: 高次項が検知されたらどうすべき？

**A**: 状況に応じて対応：

1. **精度が十分（R²>0.95）**
   - → 現状のまま使用可能
   - GPが効果的に補正している

2. **精度が不十分（R²<0.95）**
   - → データを増やす（n≥100推奨）
   - GPがより正確に高次項を捕捉

3. **データ数が少ない（n<100）**
   - → データを増やす
   - 高次項検知の信頼性向上

### Q2: 「弱い」高次項は無視してよい？

**A**: 状況による：

- **R²>0.95**: 無視してOK（実用十分）
- **R²<0.95**: データを増やすことを検討
- **予測精度が重要**: データを増やす

### Q3: 高次項検知の信頼性は？

**A**: データ量に依存：

- **n<50**: 信頼性低い（誤検知・見逃しあり）
- **n=50-100**: 中程度（強い高次項のみ検知）
- **n≥100**: 高い（ほとんどの高次項を検知）
- **n≥150**: 非常に高い（高精度検知）

### Q4: カーネルパラメータが「長すぎ」の場合は？

**A**: GP が過平滑化している可能性：

- 高次項があっても検知できない
- カーネル範囲を調整：
  ```python
  kernel = C(1.0, (1e-2, 1e2)) * RBF(1.0, (1e-2, 1e1))  # 下限を1e-2に
  ```

---

## まとめ

### ✅ この機能の利点

1. **自動検知**: ユーザーが意識せずに高次項を検知
2. **定量評価**: GP寄与度・改善率で定量化
3. **推奨事項**: データ量に応じた具体的なアクション提示
4. **信頼性**: 誤検知を最小化（カーネル評価、データ量警告）

### 🎯 使用シーン

1. **実験計画段階**: データ量の妥当性確認
2. **モデル評価**: RSM単独で十分か判断
3. **精度向上**: 高次項検知によるデータ追加判断
4. **レポート作成**: 高次項の存在を客観的に報告

### 💡 ベストプラクティス

```python
# 1. モデル訓練
model.fit(X_train, y_train)

# 2. 診断実行
info = model.diagnose(X_test, y_test)

# 3. 高次項判定
if info['higher_order_detected']:
    print(f"高次項検知: {info['higher_order_strength']}")
    print(f"GP改善率: {info['improvement']:.1f}%")

    if info['n_train'] < 100:
        print("→ データを増やすことを推奨")
    else:
        print("→ 現在のデータ量で良好に捕捉")
else:
    print("高次項なし、RSMで十分")
```

---

**実装日**: 2026-01-11
**バージョン**: RSM+GP最適化版
**テストコード**: test_higher_order_detection.py, test_strong_nonlinearity.py
